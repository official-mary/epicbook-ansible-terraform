---
- name: Create app directory
  file:
    path: "{{ app_dest }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Add safe directory for git
  command: git config --global --add safe.directory {{ app_dest }}

- name: Clone EpicBook repo from GitHub
  git:
    repo: "{{ app_repo }}"
    dest: "{{ app_dest }}"
    version: main
    force: yes

- name: Install Node.js and npm
  apt:
    name:
      - nodejs
      - npm
    state: present

- name: Install MySQL client and Python dependencies
  apt:
    name:
      - mysql-client
      - python3-mysqldb
      - python3-pymysql
    state: present

- name: Check if database is already seeded
  command: >
    mysql -h {{ db_host }} -u {{ db_user }} -p'{{ db_password }}'
    -e "SELECT COUNT(*) FROM information_schema.tables
    WHERE table_schema='{{ db_name }}';" --skip-column-names
  register: table_count
  changed_when: false
  ignore_errors: yes

- name: Run database schema
  shell: >
    mysql -h {{ db_host }} -u {{ db_user }} -p'{{ db_password }}'
    {{ db_name }} < {{ app_dest }}/db/BuyTheBook_Schema.sql
  when: table_count.stdout | trim == "0" or table_count.failed
  become: yes

- name: Run author seed
  shell: >
    mysql -h {{ db_host }} -u {{ db_user }} -p'{{ db_password }}'
    {{ db_name }} < {{ app_dest }}/db/author_seed.sql
  when: table_count.stdout | trim == "0" or table_count.failed
  become: yes

- name: Run books seed
  shell: >
    mysql -h {{ db_host }} -u {{ db_user }} -p'{{ db_password }}'
    {{ db_name }} < {{ app_dest }}/db/books_seed.sql
  when: table_count.stdout | trim == "0" or table_count.failed
  become: yes

- name: Install app dependencies
  npm:
    path: "{{ app_dest }}"

- name: Create .env file
  copy:
    dest: "{{ app_dest }}/.env"
    content: |
      PORT={{ app_port }}
      DB_HOST={{ db_host }}
      DB_USER={{ db_user }}
      DB_PASSWORD={{ db_password }}
      DB_NAME={{ db_name }}
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0640"

- name: Update config.json with DB credentials
  copy:
    dest: "{{ app_dest }}/config/config.json"
    content: |
      {
        "development": {
          "username": "{{ db_user }}",
          "password": "{{ db_password }}",
          "database": "{{ db_name }}",
          "host": "{{ db_host }}",
          "dialect": "mysql"
        },
        "production": {
          "username": "{{ db_user }}",
          "password": "{{ db_password }}",
          "database": "{{ db_name }}",
          "host": "{{ db_host }}",
          "dialect": "mysql"
        }
      }
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0640"

- name: Set ownership of app files
  file:
    path: "{{ app_dest }}"
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    recurse: yes

- name: Install pm2 globally
  npm:
    name: pm2
    global: yes

- name: Create pm2 home directory for user
  file:
    path: /var/www/.pm2
    state: directory
    owner: "{{ app_user }}"
    group: "{{ app_group }}"
    mode: "0755"
  become: yes

- name: Stop existing pm2 epicbook process
  command: pm2 delete epicbook
  become: yes
  become_user: "{{ app_user }}"
  ignore_errors: yes
  environment:
    PM2_HOME: /var/www/.pm2

- name: Start app with pm2
  command: pm2 start server.js --name epicbook
  args:
    chdir: "{{ app_dest }}"
  environment:
    PORT: "{{ app_port }}"
    PM2_HOME: /var/www/.pm2
  become: yes
  become_user: "{{ app_user }}"

- name: Save pm2 process list
  command: pm2 save
  become: yes
  become_user: "{{ app_user }}"
  environment:
    PM2_HOME: /var/www/.pm2

- name: Configure pm2 to start on boot
  command: pm2 startup systemd -u {{ app_user }} --hp /var/www/.pm2
  become: yes
  environment:
    PM2_HOME: /var/www/.pm2
